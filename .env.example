# Required — Get your API key from https://next-gen.materialsproject.org/api
MP_API_KEY=""

# Optional — Export directory (default: ./result/mp_search)
MP_EXPORT_DIR="./result/mp_search"

# Optional — UI language: zh (中文) / en (English), default: zh
MP_SEARCH_LANG="zh"
